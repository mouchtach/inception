server {
    listen 2000 default_server;
    listen [::]:2000 default_server;
    server_name _;

    root /var/www/static;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
